# M1-T05 尺寸质量测量

## Scope

建立通过实物测量得到的 Physical Ground Truth，供 M2 资产和 M3 动力学使用。

## 尺寸测量

测关节轴心，不测外壳包围盒：

```text
L1 = ||hip axis - knee axis||
L2 = ||knee axis - wheel axis||
L  = front hip axis - rear hip axis
W  = left hip axis - right hip axis
```

记录 body 的前后/左右轴距、腿部两段轴距、轮半径和轮宽。每次测量注明坐标系、工具、姿态和误差。

## 质量测量

至少记录：

```text
整机、 电池、 单个 wheel assembly、 单条 leg assembly、 body assembly
```

必要时继续拆分 upper leg、lower leg、wheel、motor、servo。实测值才是 M1 truth；CAD、STL 或现有 URDF 的值只能作为 M2 初始估计。

## 已有参考值

[[geometry-baseline]] 中的 60 mm、100 mm、40 mm 投影间距和约 1.3 kg 是 `SUPPORTED` 的名义/推导基线，不是本 Gate 的实测 PASS 证据。`sf_quad` URDF 的 mass/inertia 也必须与实测结果分开记录。

当前静态基线还包括约 66.0 mm 轮径、约 29.9 mm 轮宽、URDF 总质量 1.352539 kg，以及约 375 × 215 × 105–205 mm 的整机外形资料。这些只能作为测量前的 prior；实测表必须记录工具精度、姿态、重复读数、差值与容差。

## 输出

首版 kinematic Reference-B 可使用真实 five-bar graph 与 `60/100/100/60/40 mm` nominal geometry。exact assembled geometry 与真实 mass/inertia/CoM 仍保持 `UNKNOWN`，并明确只阻塞 higher-fidelity dynamics。

## 证据

- `doc/StackForceDog/M1_outcome/M1-G08.md`
- `doc/StackForceDog/M1_outcome/M1-G09.md`

## 更新日志

- 2026-09-11：按 fidelity ladder 冻结首版 kinematic 输入；精密 geometry 与 dynamics 保留为后续证据债务。
- 2026-09-08：同步 G08/G09 outcome，补充名义 geometry/mass prior 与实测记录合同。
